<div class="chat-info">
  <div class=chat-name-time>
    <div class="chat-name">
      <%= message.user.name%>
    </div>
    <div class="chat-time">
      <%= l message.created_at%>
    </div>
  </div>
  <div class="message">
    <%= message.content%>
  </div>
  <%= image_tag message.image.variant(resize:'500x500'), class: 'message-image' if message.image.attached?%>
  <%#= image_tagメソッド messageモデルのimageカラム(画像ファイル)を取得、variantメソッド=>指定されたサイズに変換 ファイルが存在する場合のみメソッドが実行される %>



</div>
